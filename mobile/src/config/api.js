// USB debugging via adb reverse: phone's localhost:5001 → PC's localhost:5001
// Android emulator: use http://10.0.2.2:5001
// Physical device// WiFi IP — phone and PC must be on same network
const API_URL = 'http://localhost:5001';

export default API_URL;
